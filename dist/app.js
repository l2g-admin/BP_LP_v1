const vesselFold=document.getElementById("the-vessel");if(vesselFold){const SVG_NS="http://www.w3.org/2000/svg";const wallTop=vesselFold.querySelector(".vessel__wall--top");const wallBottom=vesselFold.querySelector(".vessel__wall--bottom");const vesselFill=vesselFold.querySelector(".vessel__fill");const vesselSheen=vesselFold.querySelector(".vessel__sheen");const tagLine=vesselFold.querySelector(".vessel__tag-line");const tagDot=vesselFold.querySelector(".vessel__tag-dot");const flowGrad=vesselFold.querySelector("#vesFlow");const cellLayer=vesselFold.querySelector(".vessel__cells");const MID=130;const HALF_OPEN=58;const PINCH=40;const halfGap=(x,openness2)=>HALF_OPEN-PINCH*(1-openness2)*Math.exp(-(((x-400)/150)**2));const renderWalls=openness2=>{let top="";let bottom="";let bottomReversed="";for(let x=-20;x<=820;x+=8){const gap=halfGap(x,openness2);top+=`${top?"L":"M"} ${x} ${(MID-gap).toFixed(1)} `;bottom+=`${bottom?"L":"M"} ${x} ${(MID+gap).toFixed(1)} `;bottomReversed=`L ${x} ${(MID+gap).toFixed(1)} `+bottomReversed}wallTop.setAttribute("d",top);wallBottom.setAttribute("d",bottom);const interior=`${top}${bottomReversed}Z`;vesselFill.setAttribute("d",interior);vesselSheen.setAttribute("d",interior);const wallY=MID+halfGap(300,openness2);tagDot.setAttribute("cy",wallY.toFixed(1));tagLine.setAttribute("y1",(wallY+2).toFixed(1))};const CELL_COUNT=12;const cells=Array.from({length:CELL_COUNT},()=>{const el=document.createElementNS(SVG_NS,"circle");el.setAttribute("class","vessel__cell");el.setAttribute("cx",-40);cellLayer.appendChild(el);return{el,x:0,lane:0,v:0,r:0}});const seedCell=(cell,x)=>{cell.x=x;cell.lane=Math.random()*1.7-.85;cell.v=48+Math.random()*18;cell.r=5+Math.random()*1.2;cell.el.setAttribute("r",cell.r)};let openness=1;const ambientPair=document.createElementNS(SVG_NS,"g");[["-9","11","no-mol__n"],["11","12","no-mol__o"]].forEach(([cx,r,cls])=>{const c=document.createElementNS(SVG_NS,"circle");c.setAttribute("class",cls);c.setAttribute("cx",cx);c.setAttribute("cy","0");c.setAttribute("r",r);ambientPair.appendChild(c)});[["-9","N"],["11","O"]].forEach(([x,letter])=>{const t=document.createElementNS(SVG_NS,"text");t.setAttribute("class","no-mol__symbol");t.setAttribute("x",x);t.setAttribute("y","0");t.textContent=letter;ambientPair.appendChild(t)});ambientPair.setAttribute("opacity","0");cellLayer.parentNode.appendChild(ambientPair);const easeOutCubic=t=>1-(1-t)**3;const smooth01=t=>{const c=Math.min(Math.max(t,0),1);return c*c*(3-2*c)};const innerWallY=(x,fromTop)=>fromTop?MID-halfGap(x,openness)+12:MID+halfGap(x,openness)-12;const PULSE_MS=700;const pulses=[];const spawnPulse=(x,y)=>{const ring=document.createElementNS(SVG_NS,"circle");ring.setAttribute("class","vessel__pulse");ring.setAttribute("cx",x.toFixed(1));ring.setAttribute("cy",y.toFixed(1));cellLayer.parentNode.insertBefore(ring,ambientPair);pulses.push({el:ring,born:performance.now()})};const drawPulses=now=>{for(let i=pulses.length-1;i>=0;i-=1){const t=(now-pulses[i].born)/PULSE_MS;if(t>=1){pulses[i].el.remove();pulses.splice(i,1);continue}pulses[i].el.setAttribute("r",(7+22*easeOutCubic(t)).toFixed(1));pulses[i].el.setAttribute("opacity",(.55*(1-t)).toFixed(3))}};const drawFlight=(f,e)=>{if(!f||e<0||e>=f.rideMs+f.dwellMs+f.exitMs){if(f)f.el.style.opacity="0";return}let x;let y;let alpha;if(e<f.rideMs){const p=e/f.rideMs;x=f.startX+(f.dockX-f.startX)*p;const stream=MID+f.lane+2.5*Math.sin(e/260+f.bob);const settle=smooth01((p-.62)/.38);y=stream+(innerWallY(x,f.fromTop)-stream)*settle;alpha=Math.min(1,p/.15)}else if(e<f.rideMs+f.dwellMs){if(!f.pulsed){f.pulsed=true;spawnPulse(f.dockX,innerWallY(f.dockX,f.fromTop))}x=f.dockX;y=innerWallY(x,f.fromTop);alpha=1}else{const q=(e-f.rideMs-f.dwellMs)/f.exitMs;x=f.dockX+f.exitDist*q;y=innerWallY(x,f.fromTop);alpha=q<.6?1:1-(q-.6)/.4}f.el.style.opacity=alpha.toFixed(3);f.el.style.transform=`translate(${x.toFixed(1)}px, ${y.toFixed(1)}px)`};let ambientFlight=null;let ambientCycle=-1;const BREATHE={contract:2600,dwell:350,restore:1100,rest:1800};const BREATHE_CYCLE=BREATHE.contract+BREATHE.dwell+BREATHE.restore+BREATHE.rest;const BREATHE_DEPTH=.5;let breatheEpoch=null;const easeInOutQuad=t=>t<.5?2*t*t:1-(2-2*t)**2/2;const breathe=now=>{const since=now-breatheEpoch;const cycle=Math.floor(since/BREATHE_CYCLE);const e=since%BREATHE_CYCLE;const{contract,dwell,restore}=BREATHE;if(e<contract){openness=1-BREATHE_DEPTH*easeInOutQuad(e/contract)}else if(e<contract+dwell){openness=1-BREATHE_DEPTH}else if(e<contract+dwell+restore){openness=1-BREATHE_DEPTH+BREATHE_DEPTH*easeOutCubic((e-contract-dwell)/restore)}else{openness=1}renderWalls(openness);if(cycle!==ambientCycle){ambientCycle=cycle;const fromTop=cycle%2===0;const pinchX=380+cycle*53%70;ambientFlight={el:ambientPair,fromTop,startX:pinchX-285,dockX:pinchX,lane:fromTop?-8:8,bob:cycle%5*1.3,rideMs:1500,dwellMs:dwell,exitMs:restore+250,exitDist:280,pulsed:false}}drawFlight(ambientFlight,e-(contract-1500))};const drawCells=()=>{for(const cell of cells){const room=Math.max(halfGap(cell.x,openness)-8-cell.r,0);cell.el.setAttribute("cx",cell.x.toFixed(1));cell.el.setAttribute("cy",(MID+cell.lane*room).toFixed(1))}};renderWalls(1);cells.forEach(cell=>seedCell(cell,Math.random()*840-20));drawCells();if(matchMedia("(prefers-reduced-motion: no-preference)").matches){let vesselRaf=null;let lastNow=null;let flowShift=0;let pausedAt=null;const tick=now=>{if(lastNow===null)lastNow=now;const dt=Math.min((now-lastNow)/1e3,.05);lastNow=now;if(breatheEpoch===null)breatheEpoch=now+1500;if(now>=breatheEpoch)breathe(now);drawPulses(now);flowShift=(flowShift+38*dt)%260;flowGrad.setAttribute("gradientTransform",`translate(${flowShift.toFixed(1)} 0)`);const surge=1+1.9*openness;for(const cell of cells){const squeeze=1+.6*(1-halfGap(cell.x,openness)/HALF_OPEN);cell.x+=cell.v*surge*squeeze*dt;if(cell.x>840)seedCell(cell,-20-Math.random()*60)}drawCells();vesselRaf=requestAnimationFrame(tick)};new IntersectionObserver(entries=>{for(const entry of entries){if(entry.isIntersecting&&vesselRaf===null){if(pausedAt!==null&&breatheEpoch!==null){breatheEpoch+=performance.now()-pausedAt}pausedAt=null;lastNow=null;vesselRaf=requestAnimationFrame(tick)}else if(!entry.isIntersecting&&vesselRaf!==null){cancelAnimationFrame(vesselRaf);vesselRaf=null;pausedAt=performance.now()}}},{threshold:0}).observe(vesselFold)}}const supplyFold=document.getElementById("the-supply");if(supplyFold){const creamLine=supplyFold.querySelector(".chart__cream-line");const creamArea=supplyFold.querySelector(".chart__cream-area");const blueLine=supplyFold.querySelector(".chart__blue-line");const blueArea=supplyFold.querySelector(".chart__blue-area");const blueGlow=supplyFold.querySelector(".chart__blue-glow");const marker=supplyFold.querySelector(".chart__marker");const LX=46,RX=360,TY=34,BY=248;const A0=20,A1=70;const ageToX=a=>LX+(a-A0)/(A1-A0)*(RX-LX);const pctToY=p=>BY-p/100*(BY-TY);const ANCHORS=[[20,100],[30,75],[40,50],[50,38],[60,30],[70,25]];const catmullRom=(p0,p1,p2,p3,t)=>{const t2=t*t;const t3=t2*t;return .5*(2*p1+(-p0+p2)*t+(2*p0-5*p1+4*p2-p3)*t2+(-p0+3*p1-3*p2+p3)*t3)};const supplyAt=age=>{const last=ANCHORS.length-1;for(let i=0;i<last;i+=1){const[a1,p1]=ANCHORS[i];const[a2,p2]=ANCHORS[i+1];if(age<=a2||i===last-1){const p0=ANCHORS[Math.max(i-1,0)][1];const p3=ANCHORS[Math.min(i+2,last)][1];const t=(age-a1)/(a2-a1);return catmullRom(p0,p1,p2,p3,t)}}return ANCHORS[last][1]};const PILL_AGE=40;const DOSES=[42,48,54,60,66];const SIGMA=1.7;const SPIKE=30;const bumpAt=age=>{let b=0;for(const d of DOSES){b+=SPIKE*supplyAt(d)/100*Math.exp(-(((age-d)/SIGMA)**2))}return b};const STEP=.5;const agesTo=(from,to)=>{const out=[];for(let a=from;a<to;a+=STEP)out.push(a);out.push(to);return out};const linePath=(ages,yOf)=>ages.map((a,i)=>`${i?"L":"M"} ${ageToX(a).toFixed(1)} ${yOf(a).toFixed(1)}`).join(" ");const supplyY=a=>pctToY(supplyAt(a));const LINE_FROM=[213,79,47];const LINE_TO=[246,244,240];const lineColorAt=age=>{const t=Math.min(1,Math.max(0,(age-A0)/(A1-A0)));const[r,g,b]=LINE_FROM.map((c,i)=>Math.round(c+(LINE_TO[i]-c)*t));return`rgb(${r}, ${g}, ${b})`};const render=cur=>{const ages=agesTo(A0,cur);const cream=linePath(ages,supplyY);creamLine.setAttribute("d",cream);creamArea.setAttribute("d",`${cream} L ${ageToX(cur).toFixed(1)} ${BY} L ${ageToX(A0).toFixed(1)} ${BY} Z`);if(cur>PILL_AGE){const dAges=agesTo(PILL_AGE,cur);const top=linePath(dAges,a=>pctToY(supplyAt(a)+bumpAt(a)));const back=[...dAges].reverse().map(a=>`L ${ageToX(a).toFixed(1)} ${supplyY(a).toFixed(1)}`).join(" ");blueLine.setAttribute("d",top);blueGlow.setAttribute("d",top);blueArea.setAttribute("d",`${top} ${back} Z`)}else{blueLine.setAttribute("d","");blueGlow.setAttribute("d","");blueArea.setAttribute("d","")}marker.setAttribute("cx",ageToX(cur).toFixed(1));marker.setAttribute("cy",supplyY(cur).toFixed(1));marker.style.fill=lineColorAt(cur)};if(matchMedia("(prefers-reduced-motion: reduce)").matches){render(A1)}else{const chartEl=supplyFold.querySelector(".no-chart");const DRAW_MS=2400;const HOLD_MS=2600;const DIP_MS=550;const REARM_MS=250;let raf=null;let timers=[];let gen=0;const clear=()=>{cancelAnimationFrame(raf);timers.forEach(clearTimeout);timers=[]};const draw=myGen=>{let start=null;const step=now=>{if(gen!==myGen)return;if(start===null)start=now;const t=Math.min((now-start)/DRAW_MS,1);render(A0+(A1-A0)*t);if(t<1){raf=requestAnimationFrame(step)}else{timers.push(setTimeout(()=>dip(myGen),HOLD_MS))}};raf=requestAnimationFrame(step)};const dip=myGen=>{chartEl.classList.add("is-dipped");timers.push(setTimeout(()=>{if(gen!==myGen)return;render(A0);chartEl.classList.remove("is-dipped");timers.push(setTimeout(()=>draw(myGen),REARM_MS))},DIP_MS))};render(A0);new IntersectionObserver(entries=>{for(const entry of entries){gen++;clear();chartEl.classList.remove("is-dipped");if(entry.isIntersecting){render(A0);draw(gen)}else{render(A1)}}},{threshold:.5}).observe(chartEl)}}const sourceFold=document.getElementById("the-source");if(sourceFold&&matchMedia("(prefers-reduced-motion: no-preference)").matches){const label=sourceFold.querySelector(".conv__label");const NITRITE_AT=900;const NO_AT=1800;const HOLD_MS=2200;const EXIT_MS=650;let timers=[];let gen=0;const clearTimers=()=>{timers.forEach(clearTimeout);timers=[]};const rewindMolecule=()=>{sourceFold.classList.remove("step-nitrite","step-no","step-exit","step-enter");label.textContent="DIETARY NITRATE"};const resetScene=()=>{gen++;clearTimers();sourceFold.classList.add("is-resetting");rewindMolecule();requestAnimationFrame(()=>requestAnimationFrame(()=>sourceFold.classList.remove("is-resetting")))};const playCycle=()=>{const myGen=gen;timers.push(setTimeout(()=>{sourceFold.classList.add("step-nitrite");label.textContent="NITRITE"},NITRITE_AT));timers.push(setTimeout(()=>{sourceFold.classList.add("step-no");label.textContent="NITRIC OXIDE"},NO_AT));timers.push(setTimeout(()=>{sourceFold.classList.add("step-exit")},NO_AT+HOLD_MS));timers.push(setTimeout(()=>{rewindMolecule();sourceFold.classList.add("is-resetting","step-enter");requestAnimationFrame(()=>requestAnimationFrame(()=>{if(myGen!==gen)return;sourceFold.classList.remove("is-resetting","step-enter");playCycle()}))},NO_AT+HOLD_MS+EXIT_MS))};resetScene();new IntersectionObserver(entries=>{for(const entry of entries){if(entry.isIntersecting){resetScene();playCycle()}else{resetScene()}}},{threshold:.35}).observe(sourceFold)}const morph=document.querySelector(".morph__video");if(morph){const hero=morph.closest(".hero");const hook=hero.querySelector(".morph-hook");const claim=hero.querySelector(".headline-claim");if(matchMedia("(prefers-reduced-motion: reduce)").matches){hook.style.display="none";const holdEnd=()=>{morph.pause();if(morph.duration)morph.currentTime=morph.duration};if(morph.readyState>=1)holdEnd();else morph.addEventListener("loadedmetadata",holdEnd,{once:true})}else{const HOOK_OUT_AT=.44;const CLAIM_IN_AT=.66;const HOLD_MS=1500;const DIP_MS=600;let hookGone=false;let claimIn=false;morph.addEventListener("timeupdate",()=>{if(!morph.duration)return;const f=morph.currentTime/morph.duration;if(!hookGone&&f>=HOOK_OUT_AT){hookGone=true;hook.classList.add("is-out")}if(!claimIn&&f>=CLAIM_IN_AT){claimIn=true;claim.classList.add("is-in")}});morph.addEventListener("ended",()=>{setTimeout(()=>{morph.classList.add("is-dipped");claim.classList.remove("is-in");setTimeout(()=>{hookGone=false;claimIn=false;hook.classList.remove("is-out");morph.currentTime=0;morph.play();morph.classList.remove("is-dipped")},DIP_MS)},HOLD_MS)})}}const capsuleFold=document.getElementById("the-capsule");if(capsuleFold){const ringText=capsuleFold.querySelector(".reveal__ring-text");const RING_C=2*Math.PI*186;const fitRing=()=>{const chars=ringText.textContent.length;const spacing=parseFloat(getComputedStyle(ringText).letterSpacing)||0;const length=ringText.getComputedTextLength();ringText.style.letterSpacing=`${(spacing+(RING_C-length)/chars).toFixed(3)}px`};fitRing();document.fonts.ready.then(fitRing)}for(const block of["nobel-pop"]){const pop=document.querySelector(`.${block}`);if(!pop)continue;const trigger=pop.querySelector(`.${block}__trigger`);const card=pop.querySelector(`.${block}__card`);const closeBtn=pop.querySelector(`.${block}__close`);const setOpen=open=>{pop.classList.toggle("is-open",open);trigger.setAttribute("aria-expanded",open);card.setAttribute("aria-hidden",!open)};trigger.addEventListener("click",e=>{e.stopPropagation();setOpen(!pop.classList.contains("is-open"))});if(closeBtn)closeBtn.addEventListener("click",()=>setOpen(false));document.addEventListener("click",e=>{if(pop.classList.contains("is-open")&&!pop.contains(e.target))setOpen(false)});window.addEventListener("scroll",()=>{if(pop.classList.contains("is-open"))setOpen(false)},{passive:true});document.addEventListener("keydown",e=>{if(e.key==="Escape")setOpen(false)})}const docPops=[];document.querySelectorAll(".reveal__strip-byline[aria-controls]").forEach(btn=>{if(btn.closest("[inert]"))return;const pop=document.getElementById(btn.getAttribute("aria-controls"));if(!pop)return;const setOpen=open=>{if(open)docPops.forEach(d=>{if(d.pop!==pop)d.setOpen(false)});pop.classList.toggle("is-open",open);btn.setAttribute("aria-expanded",open);pop.setAttribute("aria-hidden",!open)};btn.addEventListener("click",e=>{e.stopPropagation();setOpen(!pop.classList.contains("is-open"))});pop.addEventListener("click",e=>e.stopPropagation());pop.querySelector(".doc-pop__close")?.addEventListener("click",()=>setOpen(false));docPops.push({pop,setOpen})});if(docPops.length){const closeAll=()=>docPops.forEach(({pop,setOpen})=>{if(pop.classList.contains("is-open"))setOpen(false)});document.addEventListener("click",closeAll);window.addEventListener("scroll",closeAll,{passive:true});document.addEventListener("keydown",e=>{if(e.key==="Escape")closeAll()})}const stripTrack=document.querySelector(".reveal__strip-track");if(stripTrack&&matchMedia("(prefers-reduced-motion: no-preference)").matches){const growTrack=()=>{const[realRun,dupRun]=stripTrack.children;const runWidth=realRun?.getBoundingClientRect().width;if(!runWidth||!dupRun)return;const runsNeeded=2*(Math.ceil(window.innerWidth/runWidth)+1);while(stripTrack.children.length<runsNeeded){stripTrack.appendChild(dupRun.cloneNode(true))}};growTrack();window.addEventListener("load",growTrack)}const stickyCta=document.querySelector(".sticky-cta");if(stickyCta&&vesselFold){const update=()=>{const top=vesselFold.getBoundingClientRect().top;stickyCta.classList.toggle("is-on",top<window.innerHeight*.65)};update();window.addEventListener("scroll",update,{passive:true});window.addEventListener("resize",update,{passive:true})}{const cta=document.querySelector(".sticky-cta__link");if(cta){const inbound=new URLSearchParams(location.search);const dest=new URL(cta.href);for(const[key,value]of inbound){if(/^utm_/i.test(key)||key==="fbclid")dest.searchParams.set(key,value)}cta.href=dest.toString();cta.addEventListener("click",()=>{if(window.fbq)fbq("trackCustom","LPCTAClick");if(window.clarity)clarity("event","cta-click")})}}{const folds=Array.from(document.querySelectorAll(".fold"));const seen=new Set;const report=()=>{folds.forEach((fold,i)=>{if(seen.has(fold))return;if(fold.getBoundingClientRect().top<window.innerHeight*.6){seen.add(fold);if(window.clarity){clarity("event",`reached-${i+1}-${fold.id||"slide"}`);if(seen.size===folds.length)clarity("event","reached-all-slides")}}})};report();window.addEventListener("scroll",report,{passive:true})}
